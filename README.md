# 📦 Application Platform – Backend & Frontend

A full-stack application built with a **Flask backend** and **Next.js frontend**, designed to manage and display application data.
The system includes an **Applications Table**, **Details View**, and a refactored backend service layer optimized for scalability and maintainability.

---

## 🚀 Features

### **Frontend (Next.js)**

* 📄 Applications table view
* 🔍 Application details page
* 🌐 API integration using environment-based configuration
* ⚛️ Modern UI components using MUI
* ⚙️ Built for extensibility (pagination, filters planned)

### **Backend (Flask)**

* 🧩 Refactored service layer for cleaner separation of concerns
* 🔐 JWT-based authentication
* 🗄️ Database models for managing application data
* ⚡ WebSocket support for real-time updates
* 🛠️ Automatic creation of default admin user via environment variables

---

## 🏗️ Tech Stack

### **Frontend**

* Next.js
* React
* TypeScript
* Material UI

### **Backend**

* Python / Flask
* SQLAlchemy
* Flask-JWT-Extended
* Socket.IO

### **Infrastructure**

* Docker & Docker Compose
* Environment-based configuration via `.env`
* Hot-reload support for development

---

## 📁 Project Structure

```
/backend        → Flask API, database models, services, WebSocket server
/frontend       → Next.js application (UI)
/docker-compose.yml
/.env           → Environment variables (not committed)
/README.md
```

---

## ⚙️ Installation & Setup

### **1️⃣ Clone the repository**

```bash
git clone <your-repo-url>
cd <repo>
```

---

## **2️⃣ Create a `.env` file**

Example:

```
JWT_SECRET_KEY=supersecretkey
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=changeme

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_EMAIL=support@example.com
SECRET_API_KEY=yourapikey
```

---

## **3️⃣ Start the application using Docker**

```bash
docker-compose up --build
```

### Services will run at:

* **Frontend:** [http://localhost:3000](http://localhost:3000)
* **Backend API:** [http://localhost:8000](http://localhost:8000)

---

## 🔑 Default Admin User

Created automatically at backend startup (only if not already present):

* **Username:** from `DEFAULT_ADMIN_USERNAME`
* **Password:** from `DEFAULT_ADMIN_PASSWORD`

Never hardcode these into the source — use environment variables only.

---

## 🧪 Development Mode

To enable hot reload:

### Backend (Flask)

```yaml
volumes:
  - ./backend:/app
```

### Frontend (Next.js)

```yaml
volumes:
  - ./frontend:/app
```

Restart docker after enabling volumes.

---

## 📡 API & WebSocket

The backend exposes:

* REST API endpoints for applications
* WebSocket event channels for real-time updates (e.g., new applications)

Documentation for specific endpoints can be added later on request.

---

## 📌 Roadmap

* [ ] Add pagination and sorting to applications table
* [ ] Implement application creation & update flows
* [ ] Add role-based admin controls
* [ ] Integrate logging and monitoring
* [ ] Deploy to production with Nginx reverse proxy

---

## 🤝 Contributing

1. Create a feature branch
2. Commit changes with clear messages
3. Open a Pull Request with description

Example PRs have been generated by ChatGPT on request.

---

## 📄 License

MIT License (or specify your desired license).
✅ A **full API documentation**
✅ A **setup guide for production deployment**

Just tell me!
